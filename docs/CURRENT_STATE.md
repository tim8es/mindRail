# Current State

**Last reconciled:** 2026-08-29

This document describes what exists, not what is intended. Roadmap items are never evidence of implementation.

## Implemented and verified

The following facts were verified through GitHub repository/API evidence and executed GitHub Actions runs:

- Repository `tim8es/mindRail` is public with `main` as the default branch.
- Development-foundation design and implementation record exist under `docs/superpowers/`.
- Branch `foundation/development-foundation` contains the authoritative project index/current-state discipline, system overview, v0.1 roadmap, ADR process, accepted ADR-0001/ADR-0002, contribution/security/engineering/agent-review documentation, BUSL-1.1 license parameters, TypeScript quality configuration, foundation sentinel test, GitHub issue/PR templates, lockfile, and permanent read-only quality workflow.
- Permanent `Quality` CI requests only `contents: read`, references no production secrets, derives Node/pnpm versions from repository package metadata, and pins GitHub-owned actions to immutable commits corresponding to `actions/checkout` v7.0.1 and `actions/setup-node` v6.4.0.
- `pnpm-lock.yaml` was generated by GitHub Actions using Node 24.19.0 and pnpm 11.24.0 after correcting one invalid dependency version discovered by the real registry.
- `pnpm install --frozen-lockfile` completed successfully and pnpm reported that the lockfile passed its supply-chain policy check for 173 entries.
- GitHub Actions Quality run `33223798035` completed successfully. Its executed `pnpm check` observed: Prettier check PASS; ESLint PASS; TypeScript `tsc` PASS; Vitest PASS with 1/1 test passing.
- GitHub Actions coverage run `33223836652` completed successfully. `pnpm test:coverage` passed with 1/1 test and reported 100% statements/branches/functions/lines for the current foundation sentinel. No project-wide coverage threshold is claimed or required yet.
- The pnpm workspace explicitly contains only the repository root (`.`); no speculative package directories are declared.
- Draft PR #1 (`chore: establish MindRail development foundation`) remains the review surface for this slice and is mergeable at the Git level.
- The standard BUSL-1.1 body in `LICENSE` was compared with the SPDX-published BUSL-1.1 text; MindRail-specific parameters are kept separately above it.
- A pre-public review of the reachable foundation change found no obvious credential-like material. Temporary write-enabled CI workflows used only for lockfile generation/formatting were removed after use; permanent CI is read-only.

## Implemented but not fully enforced

- `Quality` is executable and green, but it is not yet a mandatory repository-level merge gate on `main`.
- Issue #3 tracks enabling and verifying the minimal `main` protection/ruleset now that the repository is public.

## Planned

- MindRail domain contracts: Goal, Task, Agent, Session, Lease, Checkpoint, Permission Decision, Event.
- Vendor-neutral agent/control-plane protocol.
- Deterministic task/state transition and permission-policy engine.
- Reference runtime and persistence interfaces.
- GitHub integration.
- Cloudflare Workers/Durable Objects/D1 reference deployment.
- Codex, ChatGPT, MCP, and generic HTTP integration paths.
- Optional projections such as Google Sheets.

## External / non-technical follow-up

- MindRail-specific BUSL-1.1 parameters, Change License compatibility, licensor identity, and future contribution/dual-licensing mechanics have not received professional legal review. The repository must not imply otherwise.
- External code-contribution licensing mechanics are not finalized; see `CONTRIBUTING.md`.

## Explicit non-capabilities

MindRail currently does **not** orchestrate agents, issue runtime permissions, persist tasks, run a cloud control plane, or autonomously continue ChatGPT/Codex sessions. Those are roadmap goals.
