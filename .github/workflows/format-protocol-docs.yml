name: Format protocol docs once

on:
  push:
    branches:
      - integration/protocol-v0-1

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: integration/protocol-v0-1

      - name: Install Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: package.json
          package-manager-cache: false

      - name: Install repository pnpm
        run: npm install --global "$(node -p "require('./package.json').packageManager")"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format protocol docs
        run: pnpm exec prettier --write docs/adr/ADR-0005-control-plane-protocol-v0-1.md docs/superpowers/specs/2026-08-29-control-plane-protocol-v0-1-design.md docs/adr/README.md

      - name: Remove one-time workflow and commit
        run: |
          git rm .github/workflows/format-protocol-docs.yml
          git add docs/adr/ADR-0005-control-plane-protocol-v0-1.md docs/superpowers/specs/2026-08-29-control-plane-protocol-v0-1-design.md docs/adr/README.md
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "docs: format protocol v0.1"
          git push origin HEAD:integration/protocol-v0-1
